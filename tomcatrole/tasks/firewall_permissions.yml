---
- name: Install firewalld
  package:
    name: firewalld
    state: present


- name: Allow external HTTP(S) traffic
  firewalld:
    port: 8081/tcp
    service: "{{ item }}"
    permanent: true
    immediate: true
    state: enabled
  with_items:
    - http
    - https
  notify: restart tomcat  
