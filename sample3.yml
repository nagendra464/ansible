---
- name: remove the java package
  hosts: redhatservers
  become: yes
  tasks: 
  - name: remove the java in ubuntu machine
    yum:
      name: "{{installjava}}"
      state: "{{javaabsent}}"
      autoremove: yes
    when: ansible_os_family =="RedHat"  